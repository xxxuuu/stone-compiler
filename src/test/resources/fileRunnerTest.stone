def fib(n: Int): Int {
    if n < 2 {
        n
    } else {
        fib(n-1) + fib(n-2)
    }
}

// calc fib(33)
var start: Int = currentTime()
print(fib(33))
var end: Int = currentTime() - start
print("time:" + end)